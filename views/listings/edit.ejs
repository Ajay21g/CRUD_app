<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MoviesHub</title>
</head>
<body>
    <h3>Edit movies</h3>
    <form action="/listings/<%= listing._id %>?_method=PUT" method="POST">
        <input name="listing[name]" value="<%= listing.name %>" type="text"/>
        <br>
        <input name="listing[img]" value="<%= listing.img %>" type="text"/>
        <br>
        <textarea name="listing[summary]"value="<%= listing.summary %>" type="text"></textarea>
        <br>
        <button>Edit</button>
    </form>
</body>
</html>